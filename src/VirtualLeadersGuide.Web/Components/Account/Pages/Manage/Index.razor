@page "/Account/Manage"

<PageTitle>Profile</PageTitle>

<h3>Profile</h3>
<StatusMessage />

<RadzenRow>
    <RadzenColumn Size="12" SizeXL="6">
        <EditForm Model="Input" FormName="profile" OnValidSubmit="OnValidSubmitAsync" method="post">
            <DataAnnotationsValidator />
            <ValidationSummary class="rz-messages-error" role="alert" />
            <div class="vlg-field">
                <label for="username">Username</label>
                <input type="text" value="@username" id="username" class="rz-textbox rz-input-md" placeholder="Choose your username." disabled />
            </div>
            <div class="vlg-field">
                <label for="Input.PhoneNumber">Phone number</label>
                <InputText @bind-Value="Input.PhoneNumber" id="Input.PhoneNumber" class="rz-textbox rz-input-md" placeholder="Enter your phone number" />
                <ValidationMessage For="() => Input.PhoneNumber" class="rz-messages-error" />
            </div>
            <PrimarySubmitButton Text="Save" />
        </EditForm>
    </RadzenColumn>
</RadzenRow>
